{
    "name": "mosaic-grid",
    "version": "0.0.0",
    "private": true,
    "main": "./main.js",
    "typings": "./main.d.ts",
    "scripts": {
        "updateDependencies": "lerna bootstrap",
        "updateCoreDependencies": "lerna bootstrap --scope @ptsecurity/native-grid --scope @ptsecurity/mosaic-grid",
        "build": "lerna run build",
        "buildCore": "lerna run build --scope @ptsecurity/native-grid --scope @ptsecurity/mosaic-grid",
        "updateAndRebuild": "npm run updateDependencies && npm run build"
    },
    "devDependencies": {
        "lerna": "^3.4.1"
    }
}
